export type Vector2 = { x: number, y: number }
export type Matrix2 = { a1: number, b1: number, a2: number, b2: number }

export enum CardinalOrientation {
  NORTH = 'NORTH',
  EAST = 'EAST',
  SOUTH = 'SOUTH',
  WEST = 'WEST'
}